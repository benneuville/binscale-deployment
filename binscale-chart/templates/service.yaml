apiVersion: v1
kind: Service
metadata:
  name: {{ include "binscale-chart.fullname" . }}
  labels:
    {{- include "binscale-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "binscale-chart.selectorLabels" . | nindent 4 }}
