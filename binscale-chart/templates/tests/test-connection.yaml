apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "binscale-chart.fullname" . }}-test-connection"
  labels:
    {{- include "binscale-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "binscale-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
