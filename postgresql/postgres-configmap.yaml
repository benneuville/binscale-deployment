apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  labels:
    app: postgres
data:
  POSTGRES_DB: "event_analysis"
  POSTGRES_USER: "analyzer"
  PGDATA: "/var/lib/postgresql/data/pgdata"  # Répertoire des données